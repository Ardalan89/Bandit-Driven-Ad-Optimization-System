seed: 42

model: "ucb" 

bandit:       
  rounds: 1000

  params:           
    confidence_level: 2.0
    prior_alpha: 1.0
    prior_beta: 1.0

data:
  path: "data/raw/ads.csv"

experiment:
  log_results: true
  output_dir: "data/processed/"
